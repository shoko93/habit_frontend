{
  "name": "liff-starter-nuxtjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "NODE_OPTIONS='--openssl-legacy-provider' nuxt -p 9000",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "NODE_OPTIONS='--openssl-legacy-provider' nuxt generate"
  },
  "dependencies": {
    "@line/liff": "^2.16.0",
    "@nuxtjs/vuetify": "^1.12.3",
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7"
  }
}
